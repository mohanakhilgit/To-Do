// The main App component, now simplified to just render the router.

import React from 'react';
import AppRouter from './app/Router';

const App: React.FC = () => {
  return <AppRouter />;
}

export default App;
